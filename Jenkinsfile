@Library('roboshop-jenkins-library') _  // declaring libray
env.appName="ecomm-payment"
env.appType="python"// sending a paramter to identify type|sice we dotn want to run compile for node
ci_pipeline() //calling the pipeline-ci-jenkins-prjt-infra